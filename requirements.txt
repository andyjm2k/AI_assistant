# CATBot Python dependencies (installer + runtime).
# Reviewed against venv and venv312; trimmed to match src imports and start_all.py.
# mcp-browser-use submodule has its own pyproject.toml (uv sync there).

# Core web framework and server
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# HTTP client library
httpx>=0.25.0

# HTML parsing used by proxy fetch/scrape helpers
beautifulsoup4>=4.12.0

# Embedded Kitten TTS (OpenAI-compatible /v1/audio/speech in proxy_server)
kittentts @ https://github.com/KittenML/KittenTTS/releases/download/0.1/kittentts-0.1.0-py3-none-any.whl

# Embedded Kyutai Pocket TTS (realtime CPU model embedded in proxy_server)
pocket-tts>=1.1.1

# Environment variable management
python-dotenv>=1.0.0

# Flask and CORS (for MCP browser server)
flask>=3.0.0
flask-cors>=4.0.0

# Telegram bot integration (rate-limiter extra required for AIORateLimiter)
python-telegram-bot[rate-limiter]>=20.0

# Testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Numerical computing (for vector store). Use >=2.0 for Python 3.13 (pre-built wheels; 1.x builds from source and needs MSVC on Windows).
numpy>=2.0.0

# File operations libraries
python-docx>=1.1.0
openpyxl>=3.1.0
PyPDF2>=3.0.0
Pillow>=10.0.0
reportlab>=4.0.0
python-multipart>=0.0.9

# Cryptographic operations (for HTTPS server)
cryptography>=41.0.0

# Optional integrations that are imported directly by runtime modules
google-auth>=2.0.0
google-api-python-client>=2.0.0
huggingface-hub>=0.24.0

# MCP and browser automation (browser-use runs in mcp-browser-use submodule via uv)
mcp>=1.6.0
playwright>=1.40.0
selenium>=4.18.0
pyperclip>=1.8.0

# FastMCP HTTP client for mcp-server-browser-use (align with mcp-browser-use submodule)
fastmcp @ git+https://github.com/jlowin/fastmcp.git@3d6fd4650c4c59465fbd6983693952946259d354

# AutoGen team chat, code execution, and Studio UI (start_all.py runs autogenstudio serve)
autogen-agentchat>=0.4.0
autogen-core>=0.2.0
autogen-ext[docker]>=0.4.0
autogenstudio>=0.4.0

# LangChain (used by proxy_server for MCP connect / LLM backends)
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.2.0
langchain-google-genai>=2.0.0

# Utilities
pydantic-settings>=2.0.0
typer>=0.12.0
