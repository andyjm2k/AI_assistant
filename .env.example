# This is an example .env file. Copy it to .env and fill in your values.
# Lines starting with # are comments.

# === MCP Browser Use Configuration (supported keys only) ===
# Supported providers: openai, minimax, anthropic, google, azure_openai, groq, deepseek, cerebras, ollama, bedrock, browser_use, openrouter, vercel
# MCP_LLM_PROVIDER=google
# MCP_LLM_PROVIDER=openai
# MCP_LLM_PROVIDER=minimax
MCP_LLM_PROVIDER=ollama
# MCP_LLM_MODEL_NAME=gemini-3-flash-preview
# MCP_LLM_MODEL_NAME=MiniMax-M2.5
MCP_LLM_MODEL_NAME=llama3.2
# Optional: generic base URL override for Ollama or OpenAI-compatible providers
MCP_LLM_BASE_URL=http://localhost:11434
# Optional: generic API key override. Takes precedence over provider-specific keys.
# MCP_LLM_API_KEY=

# Provider-specific API keys (standard env vars also work: OPENAI_API_KEY, GOOGLE_API_KEY, ANTHROPIC_API_KEY, OPENROUTER_API_KEY, etc.)
# MCP_LLM_OPENAI_API_KEY=your_openai_api_key_here
# MCP_LLM_MINIMAX_API_KEY=your_minimax_api_key_here
# MCP_LLM_GOOGLE_API_KEY=your_google_api_key_here
# MCP_LLM_ANTHROPIC_API_KEY=your_anthropic_api_key_here
# MCP_LLM_OPENROUTER_API_KEY=your_openrouter_api_key_here
# MCP_LLM_AZURE_OPENAI_API_KEY=your_azure_api_key_here
# Dedicated image generation skill credentials (optional, overrides generic OpenRouter vars for Seedream)
# IMAGE_GENERATION_OPENROUTER_API_KEY=your_openrouter_api_key_here
# IMAGE_GENERATION_OPENROUTER_API_BASE=https://openrouter.ai/api/v1
# Dedicated Spotify skill credentials
SPOTIFY_CLIENT_ID=2343242345235235serwe532532525
SPOTIFY_CLIENT_SECRET=<PLACEHOLDER_FOR_USER_SECRET>
# Playback control needs a user token with user-modify-playback-state.
# Register this exact callback URI in your Spotify app settings before authorizing CATBot.
# Do not use localhost here: Spotify no longer accepts localhost aliases for redirect URIs,
# and CATBot's HTTPS certificate will not validate for https://localhost:8002.
# SPOTIFY_REFRESH_TOKEN=refresh-token-from-initial-oauth-flow
# Optional: cached bearer token. spotify_player validates this before playback and refreshes it silently when expired.
# SPOTIFY_ACCESS_TOKEN=
# Required for CATBot's local OAuth callback and re-authorization flow.
# Use the same trusted hostname as HTTPS_CERT_HOSTNAME, for example:
# SPOTIFY_REDIRECT_URI=https://your-trusted-local-hostname:8002/spotify/callback
# After the proxy is running, open https://your-trusted-local-hostname:8002/spotify/authorize once to populate
# SPOTIFY_REFRESH_TOKEN and SPOTIFY_ACCESS_TOKEN in .env automatically.
# Optional: default Spotify Connect device for playback commands.
# SPOTIFY_DEVICE_ID=
# MCP_LLM_AZURE_ENDPOINT=https://your-resource.openai.azure.com
# MCP_LLM_AZURE_API_VERSION=2024-02-01
# MCP_LLM_AWS_REGION=us-east-1

# Browser settings
MCP_BROWSER_HEADLESS=false
# MCP_BROWSER_CDP_URL=http://localhost:9222
# MCP_BROWSER_USER_DATA_DIR=C:\Users\pc\CATBot\scratch\chrome-profile
# MCP_BROWSER_PROXY_SERVER=http://host:8080
# MCP_BROWSER_PROXY_BYPASS=localhost,127.0.0.1
MCP_BROWSER_CHROMIUM_SANDBOX=true
# URL of the mcp-server-browser-use HTTP server (proxy and Flask bridge connect here).
# MCP_BROWSER_USE_HTTP_URL=http://127.0.0.1:8383/mcp

# Agent settings
MCP_AGENT_MAX_STEPS=20
MCP_AGENT_USE_VISION=true

# Deep research settings
# Example: MCP_RESEARCH_SAVE_DIRECTORY=/mnt/data/research_outputs
# Example: MCP_RESEARCH_SAVE_DIRECTORY=C:\Users\YourUser\Documents\ResearchData
MCP_RESEARCH_SAVE_DIRECTORY=C:\Users\pc\CATBot\scratch
# MCP_RESEARCH_MAX_SEARCHES=5
# MCP_RESEARCH_SEARCH_TIMEOUT=120

# MCP server settings
MCP_SERVER_LOGGING_LEVEL=INFO
# MCP_SERVER_TRANSPORT=streamable-http
# MCP_SERVER_HOST=127.0.0.1
# MCP_SERVER_PORT=8383
# MCP_SERVER_RESULTS_DIR=
# MCP_SERVER_AUTH_TOKEN=
# Monitoring dashboard log file paths
PROXY_LOG_FILE=C:\Users\pc\CATBot\logs\proxy_server.log
# Optional: browser-use server log path shown in the dashboard
BROWSER_USE_LOG_FILE=C:\Users\pc\AppData\Local\mcp-server-browser-use\server.log
# === Web UI TTS Defaults (Optional) ===
# These values will populate the web UI TTS settings when present.
# TTS_ENDPOINT=http://localhost:4123/v1
# TTS_MODEL=tts-1
# TTS_VOICE=alloy

# === Embedded Kitten TTS in proxy_server (Optional) ===
# Enable OpenAI-compatible endpoints served directly by CATBot proxy:
#   GET  /v1/audio/voices
#   POST /v1/audio/speech
# Then point TTS_ENDPOINT to your proxy base URL, e.g. http://localhost:8002
# Select the embedded backend from TTS_MODEL / TELEGRAM_TTS_MODEL:
#   kitten-tts-mini-0.8 -> embedded Kitten TTS
#   pocket-tts-realtime -> embedded Kyutai Pocket TTS
# EMBEDDED_KITTEN_TTS_ENABLED=false
# EMBEDDED_KITTEN_MODEL=KittenML/kitten-tts-nano-0.2
# EMBEDDED_KITTEN_DEFAULT_VOICE=expr-voice-2-f
# EMBEDDED_KITTEN_VOICES=expr-voice-2-f,expr-voice-3-f,expr-voice-4-m,expr-voice-5-m,expr-voice-2-m,expr-voice-5-f
# EMBEDDED_POCKET_TTS_ENABLED=false
# EMBEDDED_POCKET_MODEL=pocket-tts-realtime
# EMBEDDED_POCKET_DEFAULT_VOICE=alba
# EMBEDDED_POCKET_VOICES=alba,marius,javert,jean,fantine,cosette,eponine,azelma
# Embedded performance defaults (safe even when embedded TTS is disabled):
EMBEDDED_KITTEN_SAMPLE_RATE=24000
EMBEDDED_KITTEN_STREAM_CHUNK_BYTES=4096
EMBEDDED_KITTEN_MAX_INPUT_CHARS=220
EMBEDDED_KITTEN_CHUNK_SILENCE_MS=80
EMBEDDED_POCKET_STREAM_CHUNK_BYTES=4096
# Proxy timeout when forwarding TTS requests (seconds)
TTS_PROXY_TIMEOUT_SECONDS=180

# === Telegram Bot (Optional) ===
# See config/telegram_env_example.txt for full list and details.
# Required (bot): TELEGRAM_BOT_TOKEN, TELEGRAM_ADMIN_IDS (or TELEGRAM_ALLOW_ALL=true)
# Required (proxy): OPENAI_API_KEY or MCP_LLM_OPENAI_API_KEY for chat
# TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
# TELEGRAM_ADMIN_IDS=your_telegram_user_id
# TELEGRAM_ALLOW_ALL=false
# TELEGRAM_BACKEND_URL=http://localhost:8002
# TELEGRAM_CHAT_ENDPOINT=/v1/telegram/chat
# TELEGRAM_CHAT_MODEL=gpt-4o-mini
# TELEGRAM_CHAT_TIMEOUT=30
# TELEGRAM_CHAT_TIMEOUT_HARD_CAP=120   # Proxy-side safety cap for chat timeout
# TELEGRAM_TOOL_FOLLOWUP_TIMEOUT=45    # Timeout for post-tool "final response" calls
# TELEGRAM_BOT_CHAT_TIMEOUT_HARD_CAP=10800  # Bot-side backend request timeout cap (supports long-running tool requests)
# TELEGRAM_SECRET=  # Recommended for any production/remote setup; required for authenticated Telegram file attachments (sendTelegramFile)
# AUTOGEN_TEAM_SECRET=  # Recommended when AutoGen team tools call protected local proxy routes (/v1/proxy/fetch, browser-agent, deep-research, browser-health)
# TELEGRAM_VOICE_OUT=false
# TELEGRAM_TTS_MODEL=pocket-tts-realtime
# TELEGRAM_TTS_VOICE=alloy
# TELEGRAM_TOOLS_ENABLED=false  # Set true on proxy to enable tools (search, files, todo, memory, etc.)
# TELEGRAM_TOOLS_MAX_ITERATIONS=5  # Max tool loop iterations per message when tools enabled
# listFiles supports optional args: path (subdirectory under scratch) and recursive (true/false).
# sendTelegramFile supports subdirectory paths under scratch (e.g. images/generated/file.png).
# When tools enabled, optional for full features: BRAVE_API_KEY, NEWS_API_KEY, GOOGLE_DRIVE_*, MEMORY_*
# MEMORY_EXTRACT_MIN_USER_MESSAGES=2   # Min user messages before running extraction (default 2)
# MEMORY_EXTRACT_MIN_USER_CHARS=80     # Min total user content length before extraction (default 80)
# Todo list is stored per user in todo_data/ (or TODO_DATA_PATH). Task execution uses same backend.
# TASK_EXECUTION_MAX_ITERATIONS=200  # Max LLM+tool iterations per task execution run (default 200)
# TODO_DATA_PATH=todo_data  # Optional: directory for per-user todo JSON files (default: project_root/todo_data)

# === Context Window Controls ===
# Maximum total tokens (input + output) allowed in a single LLM request.
# The server will preflight, summarize, and retry when the request would exceed this limit.
MAX_TOKEN_LIMIT=256000
# Optional: tweak token estimation when a tokenizer is unavailable.
TOKEN_ESTIMATE_CHARS_PER_TOKEN=4
# Maximum number of entries returned in LLM-facing list-files tool replies (listFiles/list_files).
# listFiles can be scoped with path/recursive, but this cap still applies to the rendered output.
# Lower this if scratch has many files and tool responses are too large for chat context.
LIST_FILES_TOOL_MAX_ENTRIES=60

# === Large Payload Fallback ===
# Optional model to retry with when context window exhaustion occurs.
LARGE_PAYLOAD_MODEL=
# Optional: override endpoint for the large payload model (must be OpenAI-compatible).
LARGE_PAYLOAD_ENDPOINT=

# --- Codex CLI Tool (Optional) ---
# Enable/disable the Codex CLI tool endpoint
# CODEX_ENABLED=true
# Path to codex executable (defaults to "codex" if on PATH)
# CODEX_CLI_PATH=codex
# Codex sandbox mode: read-only | workspace-write | danger-full-access
# CODEX_SANDBOX_MODE=workspace-write
# Approval policy for Codex exec: untrusted | on-request | on-failure | never
# CODEX_APPROVAL_POLICY=never
# Enable Codex built-in web search tool
# CODEX_ENABLE_SEARCH=true
# Default timeout in seconds (30 minutes)
# CODEX_TIMEOUT_SECONDS=1800

BRAVE_API_KEY=x
NEWS_API_KEY=x

# --- Weather Tool (Open-Meteo) ---
# Optional override for Open-Meteo forecast endpoint used by weatherInfo tool.
OPEN_METEO_FORECAST_BASE_URL=https://api.open-meteo.com/v1/forecast
# Optional override for Open-Meteo geocoding endpoint used by weatherInfo tool.
OPEN_METEO_GEOCODING_BASE_URL=https://geocoding-api.open-meteo.com/v1/search
# Request timeout in seconds for Open-Meteo weather calls.
OPEN_METEO_TIMEOUT_SECONDS=12
# Whether Open-Meteo weather calls should honor HTTP(S)_PROXY/ALL_PROXY env vars.
# Keep false for direct network access unless you intentionally use an outbound proxy.
OPEN_METEO_TRUST_ENV=false
